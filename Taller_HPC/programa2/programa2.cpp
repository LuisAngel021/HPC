/************************
* Luis Angel Rodriguez Calderon
*Programa #2
*************************/

#include <stdio.h>
int main(){

	int x = 1, y = 0;
	if (x == 1)
	if (y == 0)
	x = 10;

	else
	x = -1;
	printf("%d\n", x);
	/* El resultado es -1 ya que como el primer condicional se cumple,
	pero el segundo no, entonces entra al else e imprime -1.*/
	return 0;
}
